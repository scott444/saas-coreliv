{
  // Typechecks the API tests, which tsconfig.json deliberately excludes so
  // they never reach the built image. noEmit, so this is a check only.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["../src/**/*.test.ts"]
}
